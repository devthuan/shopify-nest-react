import classNames from 'classnames/bind';
import styles from './Home.module.scss';
const cx = classNames.bind(styles);

const Home = () => {
    return <div className={cx('')}>dashboard</div>;
};

export default Home;
